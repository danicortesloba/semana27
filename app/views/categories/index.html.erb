<p id="notice"><%= notice %></p>

<h1>Categories</h1>


<table class="table">
  <thead>
    <tr>
      <th>Categoría</th>
      <th>Cantidad de Productos</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= link_to category.name, category_path(category.id)%></td>
        <td><%= category.products.count %></td>
        <td><%= link_to 'Edit', edit_category_path(category), class: 'btn btn-primary' %></td>
        <td><%= link_to 'Destroy', category, class: 'btn btn-warning', method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Category', new_category_path %>
